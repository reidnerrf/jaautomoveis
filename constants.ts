import { Vehicle } from './types';

// This file used to contain the initial seed data.
// This data is now managed in the database and populated via `seeder.ts`.
export const SEED_VEHICLES: Vehicle[] = [];
